mode: required
method: legacy

labels:
    step: "Filesystem"

default: btrfs

items:
    - id: btrfs
      packages: []
      name: "Btrfs"
      description: "Btrfs with subvolumes for /, /home, /nix and swap - snapshot-friendly."

    - id: ext4
      packages: []
      name: "ext4"
      description: "A single plain ext4 filesystem for the whole root."
